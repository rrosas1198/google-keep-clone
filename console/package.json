{
    "name": "app-console",
    "version": "0.0.0",
    "scripts": {
        "start": "node ./dist/main.js",
        "build": "bauen",
        "watch": "bauen watch",
        "test": "vitest",
        "test:watch": "vitest --watch",
        "test:coverage": "vitest --coverage",
        "deps:update": "taze -w"
    },
    "dependencies": {
        "@keep/core": "^0.0.1",
        "@keep/config": "^0.0.1",
        "@keep/mysql": "^0.0.1"
    },
    "devDependencies": {
        "bauen": "^0.10.9",
        "taze": "^0.8.0",
        "vitest": "^0.22.1"
    }
}
