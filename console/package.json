{
    "name": "app-console",
    "version": "0.0.0",
    "scripts": {
        "start": "node dist/main.cjs",
        "build": "unbuild",
        "test:unit": "vitest"
    },
    "dependencies": {
        "@keep/core": "^0.0.1",
        "@keep/environment": "^0.0.1",
        "@keep/mysql": "^0.0.1"
    },
    "devDependencies": {
        "@keep/config": "^0.0.1",
        "@rollup/pluginutils": "^5.0.2",
        "unbuild": "^1.0.1",
        "vitest": "^0.25.3"
    }
}
