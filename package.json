{
    "name": "root",
    "private": true,
    "workspaces": [
        "backend",
        "frontend",
        "packages/*"
    ],
    "gitHooks": {
        "commit-msg": "commitlint -e -V"
    },
    "scripts": {
        "build": "lage prepack",
        "rebuild": "lage prepack --no-cache",
        "update": "lage update && taze -w",
        "lint": "npm run lint:ts && npm run lint:scss",
        "lint:ts": "eslint */**/*.ts --fix",
        "lint:scss": "stylelint */**/*.scss --fix"
    },
    "devDependencies": {
        "@techkit/linter-config": "^0.3.7",
        "@types/node": "^18.0.5",
        "bauen": "^0.10.9",
        "eslint": "^8.20.0",
        "lage": "^1.7.1",
        "prettier": "^2.7.1",
        "taze": "^0.7.6",
        "typescript": "^4.7.4",
        "vitest": "^0.18.1",
        "yorkie": "^2.0.0"
    }
}
