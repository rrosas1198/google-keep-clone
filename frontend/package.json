{
    "name": "app-frontend",
    "version": "0.0.0",
    "scripts": {
        "start": "nuxi preview",
        "build": "nuxi build",
        "watch": "nuxi dev",
        "test": "vitest",
        "test:watch": "vitest --watch",
        "test:coverage": "vitest --coverage",
        "deps:update": "taze -w"
    },
    "dependencies": {
        "@keep/components": "^0.0.1",
        "@keep/theme": "^0.0.1",
        "nuxt": "^3.0.0-rc.5"
    },
    "devDependencies": {
        "bauen": "^0.10.9",
        "taze": "^0.7.6",
        "vitest": "^0.18.1"
    }
}
