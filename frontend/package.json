{
    "name": "app-frontend",
    "version": "0.0.0",
    "scripts": {
        "build": "nuxt build",
        "watch": "nuxt dev",
        "generate": "nuxt generate",
        "preview": "nuxt preview",
        "postinstall": "nuxt prepare",
        "test:e2e": "echo test:e2e",
        "test:unit": "vitest"
    },
    "dependencies": {
        "@keep/core": "^0.0.1",
        "@keep/platform-nuxt": "^0.0.1"
    },
    "devDependencies": {
        "@keep/components": "^0.0.1",
        "@keep/config": "^0.0.1",
        "@keep/theme": "^0.0.1",
        "nuxt": "^3.0.0",
        "vitest": "^0.25.3"
    }
}
